---
applications:
- name: rabbitmq-connector
  memory: 2048M
  disk_quota: 2048M
  stack: windows
  buildpacks:
   - hwc_buildpack
  command: cmd /c .\RabbitMQ --server.urls=http://0.0.0.0:80
  env:
    ASPNETCORE_ENVIRONMENT: Development
  services:
    - SuperRocketRabbitMQ
