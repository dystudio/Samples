---
applications:
- name: rabbitmq-connector
  memory: 256M
  disk_quota: 200M
  stack: windows
  buildpacks:
   - binary_buildpack
  env:
    ASPNETCORE_ENVIRONMENT: Development
  services:
    - SuperRocketRabbitMQ
